import VoiceAgent from '@/components/VoiceAgent';
import DemoVoiceAgent from '@/components/DemoVoiceAgent';

const Index = () => {
  return (
    <div className="min-h-screen" style={{ background: 'var(--gradient-background)' }}>
      <VoiceAgent />
    </div>
  );
};

export default Index;
